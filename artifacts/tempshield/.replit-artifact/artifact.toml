kind = "web"
previewPath = "/"
title = "TempShield"
version = "1.0.0"
id = "artifacts/tempshield"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/tempshield run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tempshield", "run", "build" ]
publicDir = "artifacts/tempshield/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
